//Первоначальное состояние redux store нашего приложения
const initialState = {
  items: [], // Полный массив объектов items в корзине
  page: { number: 1, currentItems: [] } // Текущая страница
};

export default initialState;
